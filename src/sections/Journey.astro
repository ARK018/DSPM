---
import { Image } from "astro:assets";
import classroom from "../assets/classroom.jpg";

const timelineEvents = [
  {
    year: "2000",
    description:
      "Our organization was founded with a vision to revolutionize the industry through innovation and commitment to excellence.",
    image: classroom,
    alt: "Organization founding in 2000",
  },
  {
    year: "2004",
    description:
      "We expanded our operations globally and introduced our flagship product that transformed market standards.",
    image: classroom,
    alt: "Global expansion in 2004",
  },
  {
    year: "2012",
    description:
      "We achieved industry recognition with multiple awards and celebrated reaching one million customers worldwide.",
    image: classroom,
    alt: "Awards and milestones in 2012",
  },
  {
    year: "2018",
    description:
      "Our commitment to sustainability led to a complete overhaul of our processes, setting new industry benchmarks.",
    image: classroom,
    alt: "Sustainability initiative in 2018",
  },
  {
    year: "2024",
    description:
      "Today, we continue to push boundaries with cutting-edge technology and a renewed focus on customer experience.",
    image: classroom,
    alt: "Innovation in 2024",
  },
];
---

<section id="journey" class="w-full py-20 md:py-32 lg:py-48">
  <div class="flex flex-col items-center gap-2 mb-8">
    <span
      class="text-xs sm:text-sm font-semibold tracking-wider text-black/95 border border-black rounded-full py-1 px-3 mb-4 mx-auto text-center"
      >Our journey</span
    >
    <h2
      class="text-2xl sm:text-4xl md:text-5xl font-bold mb-4 text-black/90 text-center"
    >
      Our Journey of Growth and Impact
    </h2>
    <p
      class="max-w-md sm:max-w-xl md:max-w-3xl text-base text-black/85 mx-auto text-center mb-16"
    >
      Since its humble beginnings, our educational trust has grown into a beacon
      of learning. Guided by innovation and resilience, we remain committed to
      expanding access to quality education and empowering generations to
      achieve their dreams.
    </p>
  </div>

  <div class="relative max-w-7xl container mx-auto">
    {/* Vertical Line */}
    <div
      class="absolute left-1/2 top-0 h-full w-1 bg-neutral-300 transform -translate-x-1/2 z-10"
    >
    </div>
    <div class="space-y-36 relative z-10 sm:py-2 sm:px-2.5 md:py-0">
      {
        timelineEvents.map((event, index) => (
          <div class="relative md:flex justify-between">
            {/* Year Label */}
            <div class=" md:block absolute left-1/2 transform -translate-x-1/2 sm:-top-12 md:-top-0 z-10">
              <div class="bg-white px-4 py-1 rounded-full border border-neutral-300 text-sm font-semibold shadow-md">
                {event.year}
              </div>
            </div>

            {/* Text Block */}
            <div
              class={`md:w-5/12 px-4 mt-10 md:mt-0 ${index % 2 === 0 ? "md:pr-8 md:text-right md:order-1" : "md:pl-8 md:text-left md:order-2"} `}
            >
              <h3 class="text-xl md:text-2xl font-semibold text-black mb-1">
                Event Heading {index + 1}
              </h3>
              <p class="text-sm text-neutral-600 italic mb-2">
                24 Aug, {event.year}
              </p>
              <p class="text-neutral-800 leading-relaxed">
                {event.description}
              </p>
            </div>

            {/* Image Block */}
            <div
              class={`md:w-5/12 px-4 mt-6 md:mt-0 ${index % 2 === 0 ? "md:order-2 md:pl-8" : "md:order-1 md:pr-8"}`}
            >
              <Image
                src={event.image}
                alt={event.alt}
                class="w-full h-60 object-cover rounded-lg shadow-md"
              />
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
